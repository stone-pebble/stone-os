<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.stoneos.tick"
    android:sharedUserId="android.uid.system">

    <!-- Permissions for alarm and timer functionality -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.USE_EXACT_ALARM" />

    <application
        android:label="Tick"
        android:icon="@android:drawable/ic_menu_recent_history"
        android:allowBackup="false"
        android:theme="@android:style/Theme.DeviceDefault">

        <!-- Main Tick Activity -->
        <activity
            android:name=".TickActivity"
            android:exported="true"
            android:label="Tick">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.SET_ALARM" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.SET_TIMER" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>

        <!-- BroadcastReceiver for Agent API -->
        <receiver
            android:name=".TickControlReceiver"
            android:exported="true"
            android:permission="android.permission.BROADCAST_STICKY">
            <intent-filter>
                <action android:name="com.stoneos.tick.SET_ALARM" />
                <action android:name="com.stoneos.tick.SET_TIMER" />
            </intent-filter>
        </receiver>

        <!-- AlarmReceiver for handling alarm triggers -->
        <receiver
            android:name=".AlarmReceiver"
            android:exported="false">
        </receiver>

    </application>

</manifest>
