<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Normal Permissions (automatically granted) -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.CHANGE_WIFI_STATE" />
    <uses-permission android:name="android.permission.BLUETOOTH" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <!-- Dangerous Permissions (require runtime request) -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.CALL_PHONE" />
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />
    <uses-permission android:name="android.permission.SEND_SMS" />
    <uses-permission android:name="android.permission.READ_SMS" />
    <uses-permission android:name="android.permission.RECEIVE_SMS" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.WRITE_CONTACTS" />
    <uses-permission android:name="android.permission.READ_CALL_LOG" />
    <uses-permission android:name="android.permission.READ_CALENDAR" />
    <uses-permission android:name="android.permission.WRITE_CALENDAR" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />

    <!-- LiveKit SDK Permissions - TICKET_004 -->
    <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MICROPHONE" />

    <!-- Special Permissions (require manual grant in settings) -->
    <uses-permission android:name="android.permission.WRITE_SETTINGS" />

    <!-- Package Visibility (Android 11+) - TICKET_007 -->
    <!-- Declare packages that Stone can launch and control -->
    <queries>
        <!-- Third-party apps -->
        <package android:name="com.spotify.music"/>
        <package android:name="com.google.android.apps.maps"/>
        <package android:name="com.google.android.gm"/>
        <package android:name="com.android.chrome"/>
        <package android:name="com.android.camera2"/>
        <!-- Add more controllable apps as needed -->
    </queries>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="false"
        android:networkSecurityConfig="@xml/network_security_config">

        <!-- Main Launcher Activity - TICKET_002: Native Kotlin UI -->
        <!-- This allows Stone Launcher to replace the default Android launcher -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/AppTheme"
            android:launchMode="singleTask"
            android:screenOrientation="portrait"
            android:configChanges="orientation|screenSize|keyboardHidden">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.HOME" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Chat Interface Activity - TICKET_003: Chat Interface UI -->
        <!-- Accessed via swipe left from main launcher -->
        <activity
            android:name=".ui.ChatActivity"
            android:exported="false"
            android:theme="@style/AppTheme"
            android:screenOrientation="portrait"
            android:windowSoftInputMode="adjustResize"
            android:configChanges="orientation|screenSize|keyboardHidden" />

        <!-- Stone Launcher Intent API Receiver -->
        <!-- This is the foundation of the "Headless" interface -->
        <!-- AI agents and external apps send broadcast Intents here -->
        <receiver
            android:name=".api.StoneApiReceiver"
            android:exported="true"
            android:enabled="true">
            <intent-filter>
                <!-- WiFi Control - TICKET_002 -->
                <action android:name="com.stone.launcher.action.SET_WIFI" />
                <action android:name="com.stone.launcher.action.GET_WIFI_STATE" />

                <!-- Additional Intent actions will be added here as features are implemented -->
                <!-- Each feature ticket will add its corresponding action(s) -->

                <!-- Standard category for all Stone Intent API calls -->
                <category android:name="com.stone.launcher.category.API" />
            </intent-filter>
        </receiver>

    </application>

</manifest>
